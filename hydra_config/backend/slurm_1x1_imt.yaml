# @package _global_
hydra:
  launcher:
    cpus_per_task: 10
    nodes: 1
    additional_parameters:
      gres: gpu:1
      ntasks_per_node: 1
      time: '20:00:00'
      hint: nomultithread


defaults:
  - override /hydra/launcher: submitit_slurm
  - _self_
