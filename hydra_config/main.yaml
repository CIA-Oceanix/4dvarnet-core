hydra:
  run:
    dir: dashboard/${oc.select:xp_name,.}
  sweep:
    dir: .
    subdir: dashboard/${oc.select:xp_name,.}

seed: 0

defaults:
  - file_paths: jz
  - entrypoint: ???
  - xp: ???
  - _self_


